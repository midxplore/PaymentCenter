{
  "$schema": "https://gitee.com/dotnetchina/Furion/raw/v4/schemas/v4/furion-schema.json"

  // ★ 这里**故意不写 ConfigurationScanDirectories** —— 扫描列表只由 appsettings.json
  //   一处定义，避免两份不一致。.NET 配置对**数组按下标合并**，两份写法不同时
  //   会出现「靠副作用成立」的诡异行为（历史上就踩过一次），极难归因。
  //
  // 数据库配置：Admin.NET.Application/Configuration/Database.json（唯一来源）。
  // 本文件仍需保留：Admin.NET.Test.csproj 显式 Link 了它，删文件会断链。
}
